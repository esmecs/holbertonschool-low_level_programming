# include "holberton.h"
/**
 * _islower - return value
 *
 * @c: the first character to be evalue
 * Return: value
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
